var app = {};




app.ww =$( "#video" ).width();
console.log(app.ww+"    app.ww")
app.hh= $( "#video" ).height();;
/*

defWidth = 1920;
defHeight = 1200;


    var $window;
    $window = $ ( window );
    windowWidth = $window.innerWidth ();
    windowHeight = $window.innerHeight ();
    nr_w = windowHeight / windowWidth;
    if (windowHeight < windowWidth) {
        ni_w = defWidth;
        ni_h = defHeight;

    } else {
        ni_h = defWidth;
        ni_w = defHeight;
    }
    nr_i = ni_h / ni_w;
    if (nr_i > nr_i) {
        nnuheight = windowHeight;
        nnuwidth = windowHeight / nr_i
    } else {
        nnuheight = windowWidth * nr_i;
        nnuwidth = windowWidth
    }

    nnuleft = ((windowWidth - nnuwidth) / 2);
    nnutop = ((windowHeight - nnuheight) / 2);


    scalemm = 1 + scalemmFactor * (0.00001 + windowWidth - defWidth) / defWidth;
    if (scalemm > maxScale) {
        scalemm = maxScale
    } else if ( (scalemm < minScale)) {
        scalemm = minScale
    }
    sqScale = (nnuwidth / 884) * 0.5;
    if (renew) {*/
